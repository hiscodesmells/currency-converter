server:
  port: 8080

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

exchangeRates:
  name: exchangeRates
  url: http://localhost:8080/test/
  apiKey: API_KEY